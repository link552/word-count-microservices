apiVersion: apps/v1
kind: Deployment
metadata:
  name: radware-demo-deployment
  labels:
    app: radware-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: radware-demo
  template:
    metadata:
      labels:
        app: radware-demo
    spec:
      containers:
        - name: client
          image: client:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              value: "3000"
        - name: word-ingestion-service
          image: word-ingestion-service:latest
          imagePullPolicy: IfNotPresent
        - name: word-count-service
          image: word-count-service:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: SUPABASE_URL
              value: https://xycylsxvymqsequpaflg.supabase.co
            - name: SUPABASE_KEY
              valueFrom:
                secretKeyRef:
                  name: supabase-key
                  key: SUPABASE_KEY
        - name: rabbitmq
          image: rabbitmq:4-management
          ports:
            - containerPort: 5672
